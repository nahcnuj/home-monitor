# Agent notes (home-monitor)

## Git workflow

- When a task is finished (feature, fix, docs, etc.), **commit and push to `origin`** without waiting to be asked, unless the user explicitly says not to.
- Prefer small, focused commits with clear messages (what + why).
- Do not force-push or rewrite published history unless the user requests it.
